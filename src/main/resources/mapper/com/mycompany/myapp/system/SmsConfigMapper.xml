<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.myapp.system.repository.SmsConfigRepository">

    <!-- begcode-please-regenerate-this-file 如果您不希望重新生成代码时被覆盖，将please修改为don't ！！！-->

    <!-- 通用查询映射结果 -->
    <resultMap id="baseResultMap" type="com.mycompany.myapp.system.domain.SmsConfig">
        <id column="id" property="id"/>
        <id column="id" property="id" />
        <result column="provider" property="provider" />
        <result column="sms_code" property="smsCode" />
        <result column="template_id" property="templateId" />
        <result column="access_key" property="accessKey" />
        <result column="secret_key" property="secretKey" />
        <result column="region_id" property="regionId" />
        <result column="sign_name" property="signName" />
        <result column="remark" property="remark" />
        <result column="enabled" property="enabled" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="baseColumnList">
        id,
        provider,
        sms_code,
        template_id,
        access_key,
        secret_key,
        region_id,
        sign_name,
        remark,
        enabled
    </sql>

</mapper>
